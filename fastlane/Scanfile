skip_build(true)
derived_data_path("./DerivedData")
code_coverage(true)

for_platform :ios do
  device("iPhone 13 Pro Max")
  scheme("Movies (iOS)")
end

for_platform :mac do
  scheme("Movies (macOS)")
end
